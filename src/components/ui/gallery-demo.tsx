import { PhotoGallery } from "@/components/ui/gallery";

export function GalleryPage() {
  return (
    <main className="overflow-hidden min-h-screen bg-background flex items-center justify-center">
      <PhotoGallery />
    </main>
  );
}

export default GalleryPage;
